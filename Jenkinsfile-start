pipeline {
    agent any
    environment{
        AWS_DEFAULT_REGION="us-east-1"
    }
    triggers {
        cron('58 10 * * *')
    }
    parameters {
        string(name: 'PERSON', defaultValue: 'Human', description: 'Who should I say hello to?')

        text(name: 'BIOGRAPHY', defaultValue: '', description: 'Enter some information about the person')

        booleanParam(name: 'TOGGLE', defaultValue: true, description: 'Toggle this value')
    }
    stages {
        stage('Example') {
            steps {
                echo "Hello ${params.PERSON}"

                echo "Biography: ${params.BIOGRAPHY}"

                echo "Toggle: ${params.TOGGLE}"
            }
        }
        stage('Python Script') {
            steps {
                withCredentials([aws(credentialsId: "760451896171", accessKeyVariable: 'AWS_ACCESS_KEY_ID', secretKeyVariable: 'AWS_SECRET_ACCESS_KEY')]) {
                    sh 'python3 script.py start'
                }
            }
        }
    }
}
